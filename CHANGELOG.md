# 1.2.0版本

1. 新增收益曲线页：多组合累计盈亏/累计收益率曲线、十字光标读数
2. 曲线风格与市场情绪模块（vnpy_marketsentiment）对齐：纯黑背景、右侧数值轴、灰轴白字、
   只显示一条曲线时曲线下方半透明填充
3. 新增组合汇总表：可编辑初始资金（**默认 50 万**，可逐个改）、当日/累计盈亏、累计收益率，
   最右侧“显示”列可勾选需要画在曲线上的组合（默认勾选前三个）
4. 新增成交记录页：全宽展示，支持按组合/合约筛选及导出CSV
5. 界面改为页签式布局（收益曲线 / 持仓明细 / 成交记录），顶部保留汇总条
6. 引擎新增每日盈亏快照落盘（portfolio_manager_history.json）与初始资金配置
7. 改用**交易日**口径（20:00 后的夜盘算下一交易日，周末顺延）：夜盘与其后的日盘算同一天，
   曲线不再被拆成两个点；仓位/委托映射/快照的日期键全部改为交易日
8. 同一交易日重启时**回放当日成交**，重建当前仓位与成交成本，不再出现“重启后当日盈亏偏低”
9. 行情订阅集合按当前持仓刷新（零持仓合约自动剔除），仓位与历史快照每 5 分钟额外落盘一次
10. 修复跨交易日运行时仓位与盈亏不滚动的问题
11. 其他修复：曲线配色不再随勾选漂移、未设初始资金时不画 0% 平线、未算过盈亏不覆盖当日快照、
    历史快照改为深拷贝、表格宽度按内容自适应、`key.split(",")` 加 maxsplit、关闭窗口时不再报
    “Internal C++ object already deleted”
12. 收益曲线图例图标改为纯颜色标识（单条曲线带填充时，pyqtgraph 会在图例上多画一个三角形）
13. 收益曲线 X 轴刻度固定为 yyyyMMdd 日期格式（并同步修正刻度密度，避免 8 位标签重叠）
14. 持仓明细在“本地代码”右侧新增“名称”列，名称取自主引擎合约（合约未加载时按周期补齐）
15. 成交记录在“代码”右侧新增“名称”列，列宽改为可拖动自由调整
16. 成交记录在最后一列新增“标记”列，显示委托的 mark（vnpy 的 `TradeData` 没有该字段，
    需按 `vt_orderid` 回委托缓存取）
17. 成交记录“时间”列改为显示 yyyyMMdd HH:MM:SS.mmm（原来只有时分秒，跨交易日看不出是哪一天）
18. 收益曲线图例去掉外框，只保留颜色标识与半透明底
19. 持仓明细的交易/持仓/总盈亏统一保留 2 位小数（引擎算出来的是浮点，原来会拖一长串小数）
20. 成交记录去掉“开平”列（本地以 A 股为主，不涉及开平）
21. 修复 `PortfolioChart.metric` 遮蔽 Qt 虚函数 `QPaintDevice.metric()` 的问题
    （Qt 一旦调到该虚函数就抛 “'str' object is not callable”），改名 `current_metric`
22. 持仓量不再出现负数（A 股无融券）：底仓若在本模块开始记账之前就已买入，卖出会把仓位减成负数，
    现在成交量与存档载入时统一夹到 0；`空头成交` 仍如实累计，可据此看出差额
23. 交易日口径改回**自然日**（遇周六日顺延到周一），去掉“20:00 之后算下一交易日”的期货夜盘规则：
    本地以 A 股为主、无夜盘，该规则会让当天 20:00 之后的“数据日期”跳到次日，
    并把当日盈亏也累计到次日名下（第 7 条的做法在纯 A 股环境下不适用）
24. 委托映射（`portfolio_manager_order.json`）改为随历史快照一起定期落盘（原来只在 `close()`
    里存）：成交记录与当日成交回放都依赖它，进程非正常退出时整份丢失会导致成交记录为空

# 1.1.0版本

1. vnpy框架4.0版本升级适配

# 1.0.4版本

1. 替换所有的Qt信号pyqtSignal为Signal

# 1.0.3版本

1. 调整适配PySide6新版本

# 1.0.2版本

1. 调整推送事件的数据结构为字典

# 1.0.1版本

1. 将模块的图标文件信息，改为完整路径字符串
